@model IEnumerable<BestellserviceWeb.Models.TblLeistungsbilder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LeistParent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LeistName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LeistParent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LeistName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LeistId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LeistId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LeistId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
